@EndUserText.label: 'Sales Analytics Service Binding'
@OData.publish: true
define service binding ZSB_SALES
  for service ZSD_SALES
  { 
    expose SalesOverview   as SalesOverviewV4
      with { 
        metadata: 'full';
      };
    expose SalesAggregation as SalesAggregationV4
      with { 
        metadata: 'full';
      };
  }
